# Rules moved — redirect

> **This is a redirect tombstone, not the loading index.** The global task→files loading taxonomy has been retired; loading is now per-skill.

The rules tree has moved out of `kuat-docs/rules/` and been split:

- **Passive reference** (brand, design language, content, accessibility, media-types) now lives at [`/reference`](../../reference/README.md).
- **Procedure** (role cards, checklists, loading taxonomy) is staged for the new activity skills in the repo-root `_to-skills/` holding area.

See [`/reference/MIGRATION-MAP.md`](../../reference/MIGRATION-MAP.md) for the full old→new path map.

_This file remains only so tooling that detects the repo root by this path keeps working during the migration. It is removed in Phase 2._
