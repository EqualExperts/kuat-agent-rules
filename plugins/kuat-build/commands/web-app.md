---
description: Build EE web app UI
---

Use the **create-web-app** skill (`${CLAUDE_PLUGIN_ROOT}/skills/create-web-app/SKILL.md`) to build or modify the requested Equal Experts work. Follow the skill end to end — run its intake first, load the reference slices it names, and apply its checklist before handoff.
